spring:
  application:
    name: tivio-iptv-platform

  profiles:
    active: dev

  # ==========================================
  # DATABASE CONFIGURATION - FIXED FOR POSTGRESQL
  # ==========================================
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tvboot_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:root}
    hikari:
      auto-commit: false
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
      pool-name: TvBootHikariPoolPostgres
      data-source-properties:
        ApplicationName: tvboot-iptv
        logServerErrorDetail: false

  # ==========================================
  # JPA & HIBERNATE CONFIGURATION - FIXED FOR POSTGRESQL
  # ==========================================
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
          lob:
            non_contextual_creation: true
        order_inserts: true
        order_updates: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
        default_schema: public

  # ==========================================
  # FLYWAY CONFIGURATION
  # ==========================================
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    clean-disabled: false

  # ==========================================
  # MULTIPART (FILE UPLOAD)
  # ==========================================
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
      file-size-threshold: 1KB

  # ==========================================
  # DEVTOOLS CONFIGURATION
  # ==========================================
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  # ==========================================
  # JACKSON CONFIGURATION
  # ==========================================
  jackson:
    serialization:
      indent-output: true
      write-dates-as-timestamps: false
    time-zone: UTC

  # ==========================================
  # CACHE CONFIGURATION
  # ==========================================
  cache:
    type: simple

  # ==========================================
  # VALIDATION CONFIGURATION
  # ==========================================
  web:
    resources:
      add-mappings: false

# ==========================================
# SERVER CONFIGURATION
# ==========================================
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: true
  compression:
    enabled: false

# ==========================================
# ACTUATOR CONFIGURATION
# ==========================================
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true
  health:
    db:
      enabled: true

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
logging:
  level:
    root: INFO
    com.tvboot.tivio: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
  file:
    name: logs/tvboot-dev.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n"

# ==========================================
# APP CUSTOM CONFIGURATION
# ==========================================
app:
  jwtSecret: myVeryLongSecretKeyForDevelopmentThatIsAtLeast32CharactersLong
  jwtExpirationMs: 86400000
  upload:
    logos:
      path: uploads/logos/channels/
      max-size: 5242880
  cdn:
    enabled: false
    base-url: http://localhost:8080
    logos-path: /uploads/logos/channels

# ==========================================
# SWAGGER/OPENAPI CONFIGURATION
# ==========================================
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  packages-to-scan: com.tvboot.tivio.controller